package _23_basic_sorting_algorithms;

public class _11_max_cost_to_del_array_elements {
    /** 
     * Q. Maximum Cost to remove all the array elements:
     * 
     * PseudoCode: 
     *    int totalCost = 0;
     *    sort(arr, Collections.reverseOrder()); // sort in descending order); (in desc order)
     *    for(int i = 0; i < n; i++) {
     *       totalCost += arr[i] * (i+1);     [1x, 2x, 3x, ...]
     *    } 
    */
}
