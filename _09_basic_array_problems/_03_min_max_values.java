package _09_basic_array_problems;

public class _03_min_max_values {
    /**
     * Q. Find min & max values:
     * => int min1, max1;
     *    min1 = arr[0];
     *    max1 = arr[0];
     * 
     *    for(int i = 0; i < n; i++) {
     *       if(arr[i] < min1) {
     *          min1 = arr[i];
     *       }
     * 
     *       if(arr[i] > max1) {
     *          max1 = arr[i];
     *       }
     *    }
     * 
     *    print(min1 + " " + max1);
    */
}
