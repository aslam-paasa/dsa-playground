package _09_basic_array_problems;

public class _07_sort_arr_in_ascending_order {
    /**
     * Q. Check if array is sorted in ascending order.
     * => arr = [1, 2, 2, 3, 3, 4] => sorted
     * => arr = [1, 2, 1, 3, 4]    => unsorted
     * 
     * for(int i = 1; i < n; i++) {
     *    if(arr[i] >= arr[i-1]) {
     *       // do nothing. 
     *    } else {
     *       return false;
     *    }
     * }
     * return true;
     * 
     * TC: O(N)
     * SC: O(1)
    */
}
