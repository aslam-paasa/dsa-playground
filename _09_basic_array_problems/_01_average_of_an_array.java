package _09_basic_array_problems;

public class _01_average_of_an_array {
    /**
     * Q. Average of an array:
     * => avg = sum / count
     * 
     * PseudoCode:
     * int aveage(int n) {
     *    int sum = 0;
     *    for(int i = 1; i <= n; i++) {
     *       int x = scn.nextInt();
     *       sum += x;
     *    }
     *    return sum / n;
     * }
     * */ 
}
