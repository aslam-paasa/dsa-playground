package _09_basic_array_problems;

public class _02_search_an_element_in_an_array {
    /**
     * Linear Search: O(N) [unsorted]
     * - It is usually used when the array(data) is unsorted
     * 
     * 
     * Q. Search an Element in an array:
     * => class Solution {
     *       public static int search(int arr[], int X) {
     *          int N = arr.length-1;
     *          for(int i = 0; i < N; i++) {
     *             if(arr[i] == X) {
     *                return i;
     *             }
     *          }
     *          return -1;
     *       }
     *    }
     * 
    */
}
